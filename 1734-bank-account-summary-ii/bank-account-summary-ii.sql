# Write your MySQL query statement below
select name as NAME, sum(amount) as BALANCE from Users inner join Transactions on Users.account = Transactions.account group by Transactions.account having sum(amount) > 10000;